export interface MainData {
  name: string;
  paragraph: string;
  description: string;
}
export interface MainJson {
  main: {
    name: string;
    paragraph: string;
    description: string;
  };
}
